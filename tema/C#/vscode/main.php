<?php

namespace

{
    $_REQUEST->$_FILES = $this->assertClassHasStaticAttribute($attributeName, $className);
    $_COOKIE->$http_response_header = $this->assertIsWritable($filename);
    
    $this->assertDirectoryExists($_REQUEST);

    

    $_COOKIE->zip_open = $this->assertTrue($condition);
    $_COOKIE->zip_close = $this->assertTrue($condition);

    private static{
        @$this->assertXmlStringEqualsXmlString($expectedXml, $actualXml);
    }
}