async (params) => {
   class name {
    constructor(parameters) {
      console.warn(structuredClone);
      RTCDataChannelEvent.console.warn(BaseAudioContext);
      async (params) => {
         CSSNumericValue
      }
    }
   }   
}